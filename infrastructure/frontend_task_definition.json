[
    {
        "name": "${service_name}",
        "image": "${image}",
        "cpu": ${cpu},
        "memory": ${memoryReservation},
        "portMappings": [
            {
                "containerPort": ${containerPort},
                "hostPort": 0,
                "protocol": "tcp"
            }
        ]
    }
]